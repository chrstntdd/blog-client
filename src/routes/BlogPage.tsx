import * as React from 'react';

interface PropTypes {}
interface StateType {}

export default class BlogPage extends React.Component<PropTypes, StateType> {
  render() {
    return (
      <section>
        <h1> BLOG PAGE</h1>
      </section>
    );
  }
}
