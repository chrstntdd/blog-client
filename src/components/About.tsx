import React, { Component } from 'react';

interface PropTypes {}
interface StateType {}

export default class About extends Component<PropTypes, StateType> {
  render() {
    return (
      <section>
        <h1> ABOUT PAGE</h1>
      </section>
    );
  }
}
