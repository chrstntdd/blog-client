import { h } from 'preact';
import styles from './footer.scss'

const Footer = () => {
  return (
    <footer class={styles.footer}>
      <h3>FOOTER AREA</h3>
    </footer>
  );
};

export default Footer;
