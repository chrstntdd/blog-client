import { h, Component } from 'preact';

interface PropTypes {}
interface StateType {}

export default class Contact extends Component<PropTypes, StateType> {
  render() {
    return (
      <section>
        <h1> CONTACT PAGE</h1>
      </section>
    );
  }
}
